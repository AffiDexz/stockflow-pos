# MySQL connection string (Prisma).
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://user:password@localhost:3306/stockflow"

# Port the API listens on
PORT=4000

# Allowed frontend origin for CORS (use your deployed frontend URL in production)
CORS_ORIGIN="*"
